use wasm_bindgen::prelude::*;

// use html_grid::*;
// use html_data::*;
// use rusty_planner::*;

// mod html_data;
// mod html_grid;
// mod rusty_planner;

#[wasm_bindgen(start)]
pub fn run() -> Result<(), JsValue> {
    //RustyPlanner::new("rustyPlannerContainer");
    
    Ok(())
}
